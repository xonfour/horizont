package framework.model.type;

/**
 * Types of data element.
 *
 * @author Stefan Werner
 */
public enum DataElementType {
	FILE, FOLDER, OTHER, NONEXISTENT_OR_UNKNOWN, UNKNOWN // TODO: What about the last 3 types? Do we need them? Do we need more?
}
