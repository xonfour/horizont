package framework.model.event.type;

/**
 * Types of module update events.
 *
 * @author Stefan Werner
 */
public enum ModuleUpdateEventType {
	ADD, FAIL_INIT, FAIL_RESPOND, FAIL_START, FAIL_STOP, REMOVE, UPDATE
}
