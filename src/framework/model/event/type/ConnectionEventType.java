package framework.model.event.type;

/**
 * Types of connection events.
 *
 * @author Stefan Werner
 */
public enum ConnectionEventType {
	ADD, BUSY, CONNECTED, DISCONNECTED, ERROR, IDLE, REMOVED, DATAUPDATE
}
